- Add opacity as an optional parameter.
- Change luminosity by converting first to HSL values and then back to RGB like
  Sass.
- Convert RGB -> HSV and HSV -> RGB.
- Convert RGB -> HSL and HSL -> RGB.
